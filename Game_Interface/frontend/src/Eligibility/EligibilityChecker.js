// Checks if a tapped RFID is eligible for physical games
export function checkEligibility(rfidTag) {
  // Replace with real logic, e.g., check against backend or rules
  return rfidTag ? true : false;
}
